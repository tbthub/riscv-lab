#ifndef __MATH_H__
#define __MATH_H__
#include "std/stddef.h"
extern uint64 next_power_of_2(uint64 n);
extern uint32 calculate_order(uint32 size);
extern uint32 math_log(uint32 num, uint16 base);
extern uint32 math_pow(uint32 base, uint16 exponent);
#endif